@startuml
skinparam backgroundColor #FFFFFF
skinparam defaultFontSize 13
skinparam shadowing false
skinparam roundCorner 8
skinparam activityBackgroundColor #f1f5f9
skinparam activityBorderColor #3b82f6
skinparam arrowColor #3b82f6

title Widevine CDM 4.10.2934 Key Lifecycle

start

:License Response (1210B)
arrives via UpdateSession();

:CDM decrypts license internally;

:Content Key **K** (16B)
BRIEFLY on stack frame;
note right: Transient - exists < 1ms

:Immediately XOR-blinded
K_blinded = K XOR M
(M = session-derived mask);

:Heap storage: **K_blinded**
(17 key_id indexed locations);
note right: Bare key NEVER in heap

:--- Per Decrypt() call ---;

:Stack restore: K = K_blinded XOR M
(current stack frame only);

:White-box AES rounds
(OLLVM virtualized, 97% CPU);

:K and round keys **ZEROED**
(before function return);
note right: Zeroed by explicit memset

:**DecryptAndDecodeFrame output**
Decrypted YUV plaintext
<-- CAPTURED HERE;
note right #dcfce7: Our vtable hook captures the decoded video frames

stop

@enduml