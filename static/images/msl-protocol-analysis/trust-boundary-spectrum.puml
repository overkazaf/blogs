@startuml
skinparam backgroundColor #FFFFFF
skinparam defaultFontName "Segoe UI,Helvetica,Arial,sans-serif"
skinparam defaultFontSize 13
skinparam defaultFontColor #1e293b
skinparam shadowing false
skinparam roundCorner 10
skinparam arrowColor #3b82f6
skinparam arrowThickness 2
skinparam rectangle {
  BorderThickness 2
}

title 内容保护的信任边界谱系 —— 越往下，单一凭据泄露通常越难复用

rectangle "**① 国内音乐  .ncm / .qmc**\n信任边界 = **文件**（密钥内置客户端）\n单点破解 → 通用离线解密器，一劳永逸" as A #fecaca
rectangle "**② 长视频·历史容器  .kux / .qsv / .qlv·.tdl**\n信任边界 = **文件**（容器混淆 / 伪加密）\n单点破解 → 离线 remux 还原（老版本；新版已失效）" as B #fed7aa
rectangle "**③ 长视频·在线签名授权  cKey / UPS 签名 + 会员 cookie**\n信任边界 = **token + 请求签名**（服务端下发时效 URL）\n单点破解 → 逆签名算法 + 持 token 取下发流" as C #fef08a
rectangle "**④ 长视频·商业 / 自研 DRM  DRM-S · ChinaDRM · Widevine**\n信任边界 = **DRM 实体能力**（可为软件级或硬件 TEE）\n单点泄露 → 结果受设备等级、授权和输出路径约束" as D #bbf7d0
rectangle "**⑤ Netflix MSL  Widevine + MSL Key Exchange**\n信任边界 = **MSL 实体 + 会话能力**（控制面也进入加密信封）\n单点泄露 → 仍需 session key 或活 oracle；复用程度取决于配置" as E #e9d5ff

A -down-> B
B -down-> C
C -down-> D
D -down-> E

note right of B #fffbeb
第一代：私有容器 + 弱加密。
挡的是普通用户，
挡不住确定性攻击者。
end note

note right of D #ecfdf5
产品名称不能直接代表部署等级：
同一 DRM 在 L1/L3、TEE/软件路径上的
密钥可导出性、输出保护和克隆成本不同。
end note

note right of E #f3e8ff
本文可证明的是 MSL 将控制面纳入
实体 / 用户 / session 的密码学上下文；
是否达到硬件设备绑定，取决于实际
entity authentication 与 Key Exchange。
end note

@enduml
